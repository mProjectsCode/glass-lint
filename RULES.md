<!-- Generated by `make generate-rules`; do not edit manually. -->

# Glass Lint rules

This document is generated from the provider rule catalogs. Each rule lists the exact matcher queries that produce findings.

### `js` catalog (7 rules)

#### `js:dynamic-code.eval`

Default severity: `warning`

Queries:

- Emit `eval` when a call to the global `eval`.
- Emit `Function` when a call to the global `Function`.
- Emit `Function` when a constructor call to the global `Function`.

#### `js:network.url-construction`

Default severity: `info`

Queries:

- Emit `URL` when a constructor call to the global `URL`.
- Emit `URLSearchParams` when a constructor call to the global `URLSearchParams`.
- Emit `URL.parse` when a member call to `URL.parse` on the rooted path `URL.parse`.
- Emit `URL.canParse` when a member call to `URL.canParse` on the rooted path `URL.canParse`.
- Emit `URL.createObjectURL` when a member call to `URL.createObjectURL` on the rooted path `URL.createObjectURL`.
- Emit `URL.revokeObjectURL` when a member call to `URL.revokeObjectURL` on the rooted path `URL.revokeObjectURL`.
- Emit `http://` when a string reference containing `http://`.
- Emit `https://` when a string reference containing `https://`.

#### `js:network.private-address`

Default severity: `info`

Queries:

- Emit `__glass_lint_private_network_literal__` when a string reference containing `__glass_lint_private_network_literal__`.

#### `js:network.service-indicator`

Default severity: `info`

Queries:

- Emit `openai` when an import of the package specifier `openai`.
- Emit `firebase` when an import of the package specifier `firebase`.
- Emit `dropbox` when an import of the package specifier `dropbox`.
- Emit `@supabase/supabase-js` when an import of the package specifier `@supabase/supabase-js`.
- Emit `@aws-sdk/client-s3` when an import of the package specifier `@aws-sdk/client-s3`.
- Emit `@aws-sdk/client-dynamodb` when an import of the package specifier `@aws-sdk/client-dynamodb`.
- Emit `@aws-sdk/client-lambda` when an import of the package specifier `@aws-sdk/client-lambda`.
- Emit `@google-cloud/storage` when an import of the package specifier `@google-cloud/storage`.
- Emit `@google-cloud/firestore` when an import of the package specifier `@google-cloud/firestore`.
- Emit `@google-cloud/pubsub` when an import of the package specifier `@google-cloud/pubsub`.
- Emit `@azure/storage-blob` when an import of the package specifier `@azure/storage-blob`.
- Emit `@azure/identity` when an import of the package specifier `@azure/identity`.
- Emit `stripe` when an import of the package specifier `stripe`.
- Emit `@stripe/stripe-js` when an import of the package specifier `@stripe/stripe-js`.
- Emit `twilio` when an import of the package specifier `twilio`.
- Emit `@twilio/voice-sdk` when an import of the package specifier `@twilio/voice-sdk`.
- Emit `@sendgrid/mail` when an import of the package specifier `@sendgrid/mail`.
- Emit `mailgun.js` when an import of the package specifier `mailgun.js`.
- Emit `@octokit/rest` when an import of the package specifier `@octokit/rest`.
- Emit `api.openai.com` when a string reference containing `api.openai.com`.
- Emit `amazonaws.com` when a string reference containing `amazonaws.com`.
- Emit `supabase.co` when a string reference containing `supabase.co`.
- Emit `api.stripe.com` when a string reference containing `api.stripe.com`.
- Emit `api.twilio.com` when a string reference containing `api.twilio.com`.
- Emit `api.sendgrid.com` when a string reference containing `api.sendgrid.com`.
- Emit `api.mailgun.net` when a string reference containing `api.mailgun.net`.
- Emit `slack.com/api` when a string reference containing `slack.com/api`.

#### `js:network.telemetry-indicator`

Default severity: `info`

Queries:

- Emit `@sentry/browser` when an import of the package specifier `@sentry/browser`.
- Emit `@sentry/node` when an import of the package specifier `@sentry/node`.
- Emit `posthog-js` when an import of the package specifier `posthog-js`.
- Emit `mixpanel-browser` when an import of the package specifier `mixpanel-browser`.
- Emit `@sentry/electron` when an import of the package specifier `@sentry/electron`.
- Emit `@sentry/react` when an import of the package specifier `@sentry/react`.
- Emit `@sentry/vue` when an import of the package specifier `@sentry/vue`.
- Emit `@sentry/nextjs` when an import of the package specifier `@sentry/nextjs`.
- Emit `@opentelemetry/api` when an import of the package specifier `@opentelemetry/api`.
- Emit `@opentelemetry/sdk-node` when an import of the package specifier `@opentelemetry/sdk-node`.
- Emit `@opentelemetry/sdk-trace-web` when an import of the package specifier `@opentelemetry/sdk-trace-web`.
- Emit `@opentelemetry/exporter-trace-otlp-http` when an import of the package specifier `@opentelemetry/exporter-trace-otlp-http`.
- Emit `@segment/analytics-next` when an import of the package specifier `@segment/analytics-next`.
- Emit `analytics` when an import of the package specifier `analytics`.
- Emit `@amplitude/analytics-browser` when an import of the package specifier `@amplitude/analytics-browser`.
- Emit `@datadog/browser-rum` when an import of the package specifier `@datadog/browser-rum`.
- Emit `@logrocket/react` when an import of the package specifier `@logrocket/react`.
- Emit `fullstory` when an import of the package specifier `fullstory`.
- Emit `sentry.io` when a string reference containing `sentry.io`.
- Emit `google-analytics.com` when a string reference containing `google-analytics.com`.
- Emit `app.posthog.com` when a string reference containing `app.posthog.com`.
- Emit `api.segment.io` when a string reference containing `api.segment.io`.
- Emit `browser-intake-datadoghq.com` when a string reference containing `browser-intake-datadoghq.com`.
- Emit `api.amplitude.com` when a string reference containing `api.amplitude.com`.
- Emit `logrocket.com` when a string reference containing `logrocket.com`.

#### `js:network.header-indicator`

Default severity: `info`

Queries:

- Emit `fetch` when a call to the global `fetch` with argument 1 matches an object with keys `Authorization`, `Cookie`, `User-Agent`, `X-API-Key`, `authorization`, `cookie`, `user-agent`, `x-api-key`.
- Emit `User-Agent` when a string reference containing `User-Agent`.
- Emit `user-agent` when a string reference containing `user-agent`.
- Emit `USER-AGENT` when a string reference containing `USER-AGENT`.
- Emit `Authorization` when a string reference containing `Authorization`.
- Emit `authorization` when a string reference containing `authorization`.
- Emit `AUTHORIZATION` when a string reference containing `AUTHORIZATION`.
- Emit `Cookie` when a string reference containing `Cookie`.
- Emit `COOKIE` when a string reference containing `COOKIE`.
- Emit `Proxy-Authorization` when a string reference containing `Proxy-Authorization`.
- Emit `PROXY-AUTHORIZATION` when a string reference containing `PROXY-AUTHORIZATION`.
- Emit `X-API-Key` when a string reference containing `X-API-Key`.
- Emit `x-api-key` when a string reference containing `x-api-key`.
- Emit `Api-Key` when a string reference containing `Api-Key`.
- Emit `api-key` when a string reference containing `api-key`.
- Emit `API-KEY` when a string reference containing `API-KEY`.
- Emit `X-Auth-Token` when a string reference containing `X-Auth-Token`.
- Emit `x-auth-token` when a string reference containing `x-auth-token`.
- Emit `X-Access-Token` when a string reference containing `X-Access-Token`.
- Emit `x-access-token` when a string reference containing `x-access-token`.
- Emit `X-Client-Token` when a string reference containing `X-Client-Token`.
- Emit `x-client-token` when a string reference containing `x-client-token`.
- Emit `X-API-Token` when a string reference containing `X-API-Token`.
- Emit `x-api-token` when a string reference containing `x-api-token`.

#### `js:dynamic-code.string-timer`

Default severity: `warning`

Queries:

- Emit `setTimeout` when a call to the global `setTimeout` with argument 0 matches any static string.
- Emit `setInterval` when a call to the global `setInterval` with argument 0 matches any static string.

### `browser` catalog (16 rules)

#### `browser:browser.clipboard-read`

Default severity: `info`

Queries:

- Emit `navigator.clipboard.read` when a member call to `navigator.clipboard.read` on the rooted path `navigator.clipboard.read`.
- Emit `navigator.clipboard.readText` when a member call to `navigator.clipboard.readText` on the rooted path `navigator.clipboard.readText`.
- Emit `document.execCommand` when a member call to `document.execCommand` on the rooted path `document.execCommand` with argument 0 matches one of the exact strings `paste`.

#### `browser:browser.clipboard-write`

Default severity: `info`

Queries:

- Emit `navigator.clipboard.write` when a member call to `navigator.clipboard.write` on the rooted path `navigator.clipboard.write`.
- Emit `navigator.clipboard.writeText` when a member call to `navigator.clipboard.writeText` on the rooted path `navigator.clipboard.writeText`.
- Emit `document.execCommand` when a member call to `document.execCommand` on the rooted path `document.execCommand` with argument 0 matches one of the exact strings `copy`, `cut`.

#### `browser:browser.persistent-storage`

Default severity: `info`

Queries:

- Emit `document.cookie` when a member read of `document.cookie` on the rooted path `document.cookie`.
- Emit `document.cookie` when a property write to `document.cookie` on the rooted path `document.cookie`.
- Emit `window.cookie` when a property write to `window.cookie` on the rooted path `window.cookie`.
- Emit `self.cookie` when a property write to `self.cookie` on the rooted path `self.cookie`.
- Emit `globalThis.cookie` when a property write to `globalThis.cookie` on the rooted path `globalThis.cookie`.
- Emit `localStorage.length` when a member read of `localStorage.length` on the rooted path `localStorage.length`.
- Emit `sessionStorage.length` when a member read of `sessionStorage.length` on the rooted path `sessionStorage.length`.
- Emit `localStorage.getItem` when a member call to `localStorage.getItem` on the rooted path `localStorage.getItem`.
- Emit `localStorage.setItem` when a member call to `localStorage.setItem` on the rooted path `localStorage.setItem`.
- Emit `localStorage.removeItem` when a member call to `localStorage.removeItem` on the rooted path `localStorage.removeItem`.
- Emit `localStorage.clear` when a member call to `localStorage.clear` on the rooted path `localStorage.clear`.
- Emit `localStorage.key` when a member call to `localStorage.key` on the rooted path `localStorage.key`.
- Emit `sessionStorage.getItem` when a member call to `sessionStorage.getItem` on the rooted path `sessionStorage.getItem`.
- Emit `sessionStorage.setItem` when a member call to `sessionStorage.setItem` on the rooted path `sessionStorage.setItem`.
- Emit `sessionStorage.removeItem` when a member call to `sessionStorage.removeItem` on the rooted path `sessionStorage.removeItem`.
- Emit `sessionStorage.clear` when a member call to `sessionStorage.clear` on the rooted path `sessionStorage.clear`.
- Emit `sessionStorage.key` when a member call to `sessionStorage.key` on the rooted path `sessionStorage.key`.
- Emit `indexedDB.open` when a member call to `indexedDB.open` on the rooted path `indexedDB.open`.
- Emit `indexedDB.deleteDatabase` when a member call to `indexedDB.deleteDatabase` on the rooted path `indexedDB.deleteDatabase`.
- Emit `indexedDB.databases` when a member call to `indexedDB.databases` on the rooted path `indexedDB.databases`.
- Emit `caches.open` when a member call to `caches.open` on the rooted path `caches.open`.
- Emit `caches.match` when a member call to `caches.match` on the rooted path `caches.match`.
- Emit `caches.has` when a member call to `caches.has` on the rooted path `caches.has`.
- Emit `caches.delete` when a member call to `caches.delete` on the rooted path `caches.delete`.
- Emit `caches.keys` when a member call to `caches.keys` on the rooted path `caches.keys`.
- Emit `navigator.storage.persist` when a member call to `navigator.storage.persist` on the rooted path `navigator.storage.persist`.
- Emit `navigator.storage.persisted` when a member call to `navigator.storage.persisted` on the rooted path `navigator.storage.persisted`.
- Emit `navigator.storage.estimate` when a member call to `navigator.storage.estimate` on the rooted path `navigator.storage.estimate`.
- Emit `navigator.storage.getDirectory` when a member call to `navigator.storage.getDirectory` on the rooted path `navigator.storage.getDirectory`.
- Emit `navigator.storage.getDirectory` when all of: event $0 is selected; event $0 is a member call to `getFileHandle`; event $0 has identity the rooted path `navigator.storage.getDirectory`; object $1 is returned by the rooted path `navigator.storage.getDirectory`; event $0 uses object $1 as its member receiver.
- Emit `navigator.storage.getDirectory` when all of: event $0 is selected; event $0 is a member call to `getDirectoryHandle`; event $0 has identity the rooted path `navigator.storage.getDirectory`; object $1 is returned by the rooted path `navigator.storage.getDirectory`; event $0 uses object $1 as its member receiver.
- Emit `navigator.storage.getDirectory` when all of: event $0 is selected; event $0 is a member call to `removeEntry`; event $0 has identity the rooted path `navigator.storage.getDirectory`; object $1 is returned by the rooted path `navigator.storage.getDirectory`; event $0 uses object $1 as its member receiver.
- Emit `navigator.storage.getDirectory` when all of: event $0 is selected; event $0 is a member call to `entries`; event $0 has identity the rooted path `navigator.storage.getDirectory`; object $1 is returned by the rooted path `navigator.storage.getDirectory`; event $0 uses object $1 as its member receiver.
- Emit `cookieStore.get` when a member call to `cookieStore.get` on the rooted path `cookieStore.get`.
- Emit `cookieStore.getAll` when a member call to `cookieStore.getAll` on the rooted path `cookieStore.getAll`.
- Emit `cookieStore.set` when a member call to `cookieStore.set` on the rooted path `cookieStore.set`.
- Emit `cookieStore.delete` when a member call to `cookieStore.delete` on the rooted path `cookieStore.delete`.

#### `browser:browser.permissions-geolocation`

Default severity: `info`

Queries:

- Emit `navigator.geolocation.getCurrentPosition` when a member call to `navigator.geolocation.getCurrentPosition` on the rooted path `navigator.geolocation.getCurrentPosition`.
- Emit `navigator.geolocation.watchPosition` when a member call to `navigator.geolocation.watchPosition` on the rooted path `navigator.geolocation.watchPosition`.

#### `browser:browser.permissions-hardware`

Default severity: `info`

Queries:

- Emit `navigator.hid.requestDevice` when a member call to `navigator.hid.requestDevice` on the rooted path `navigator.hid.requestDevice`.
- Emit `navigator.serial.requestPort` when a member call to `navigator.serial.requestPort` on the rooted path `navigator.serial.requestPort`.
- Emit `navigator.usb.requestDevice` when a member call to `navigator.usb.requestDevice` on the rooted path `navigator.usb.requestDevice`.

#### `browser:browser.permissions-media`

Default severity: `info`

Queries:

- Emit `navigator.mediaDevices.getUserMedia` when a member call to `navigator.mediaDevices.getUserMedia` on the rooted path `navigator.mediaDevices.getUserMedia`.
- Emit `navigator.mediaDevices.getDisplayMedia` when a member call to `navigator.mediaDevices.getDisplayMedia` on the rooted path `navigator.mediaDevices.getDisplayMedia`.
- Emit `navigator.mediaDevices.enumerateDevices` when a member call to `navigator.mediaDevices.enumerateDevices` on the rooted path `navigator.mediaDevices.enumerateDevices`.

#### `browser:browser.permissions-bluetooth`

Default severity: `info`

Queries:

- Emit `navigator.bluetooth.requestDevice` when a member call to `navigator.bluetooth.requestDevice` on the rooted path `navigator.bluetooth.requestDevice`.

#### `browser:browser.permissions-notifications`

Default severity: `info`

Queries:

- Emit `Notification.requestPermission` when a member call to `Notification.requestPermission` on the rooted path `Notification.requestPermission`.
- Emit `self.registration.showNotification` when a member call to `self.registration.showNotification` on the rooted path `self.registration.showNotification`.
- Emit `registration.showNotification` when a member call to `registration.showNotification` on the rooted path `registration.showNotification`.
- Emit `Notification` when a constructor call to the global `Notification`.

#### `browser:browser.permissions-query`

Default severity: `info`

Queries:

- Emit `navigator.permissions.query` when a member call to `navigator.permissions.query` on the rooted path `navigator.permissions.query`.

#### `browser:browser.environment`

Default severity: `info`

Queries:

- Emit `navigator.userAgent` when a member read of `navigator.userAgent` on the rooted path `navigator.userAgent`.
- Emit `navigator.userAgentData` when a member read of `navigator.userAgentData` on the rooted path `navigator.userAgentData`.
- Emit `navigator.globalPrivacyControl` when a member read of `navigator.globalPrivacyControl` on the rooted path `navigator.globalPrivacyControl`.
- Emit `navigator.platform` when a member read of `navigator.platform` on the rooted path `navigator.platform`.
- Emit `navigator.language` when a member read of `navigator.language` on the rooted path `navigator.language`.
- Emit `screen.width` when a member read of `screen.width` on the rooted path `screen.width`.
- Emit `screen.height` when a member read of `screen.height` on the rooted path `screen.height`.
- Emit `screen.availWidth` when a member read of `screen.availWidth` on the rooted path `screen.availWidth`.
- Emit `screen.availHeight` when a member read of `screen.availHeight` on the rooted path `screen.availHeight`.
- Emit `screen.colorDepth` when a member read of `screen.colorDepth` on the rooted path `screen.colorDepth`.
- Emit `screen.pixelDepth` when a member read of `screen.pixelDepth` on the rooted path `screen.pixelDepth`.
- Emit `screen.orientation` when a member read of `screen.orientation` on the rooted path `screen.orientation`.
- Emit `screen.availLeft` when a member read of `screen.availLeft` on the rooted path `screen.availLeft`.
- Emit `screen.availTop` when a member read of `screen.availTop` on the rooted path `screen.availTop`.
- Emit `navigator.languages` when a member read of `navigator.languages` on the rooted path `navigator.languages`.
- Emit `navigator.hardwareConcurrency` when a member read of `navigator.hardwareConcurrency` on the rooted path `navigator.hardwareConcurrency`.
- Emit `navigator.deviceMemory` when a member read of `navigator.deviceMemory` on the rooted path `navigator.deviceMemory`.
- Emit `navigator.vendor` when a member read of `navigator.vendor` on the rooted path `navigator.vendor`.
- Emit `navigator.cookieEnabled` when a member read of `navigator.cookieEnabled` on the rooted path `navigator.cookieEnabled`.
- Emit `navigator.maxTouchPoints` when a member read of `navigator.maxTouchPoints` on the rooted path `navigator.maxTouchPoints`.
- Emit `navigator.doNotTrack` when a member read of `navigator.doNotTrack` on the rooted path `navigator.doNotTrack`.
- Emit `navigator.webdriver` when a member read of `navigator.webdriver` on the rooted path `navigator.webdriver`.
- Emit `navigator.pdfViewerEnabled` when a member read of `navigator.pdfViewerEnabled` on the rooted path `navigator.pdfViewerEnabled`.
- Emit `navigator.onLine` when a member read of `navigator.onLine` on the rooted path `navigator.onLine`.
- Emit `navigator.connection.effectiveType` when a member read of `navigator.connection.effectiveType` on the rooted path `navigator.connection.effectiveType`.
- Emit `navigator.connection.rtt` when a member read of `navigator.connection.rtt` on the rooted path `navigator.connection.rtt`.
- Emit `navigator.connection.downlink` when a member read of `navigator.connection.downlink` on the rooted path `navigator.connection.downlink`.
- Emit `navigator.connection.saveData` when a member read of `navigator.connection.saveData` on the rooted path `navigator.connection.saveData`.

#### `browser:browser.global-input-hook`

Default severity: `info`

Queries:

- Emit `document.addEventListener` when a member call to `document.addEventListener` on the rooted path `document.addEventListener` with argument 0 matches one of the exact strings `beforeinput`, `compositionend`, `compositionstart`, `compositionupdate`, `contextmenu`, `copy`, `cut`, `dragend`, `dragover`, `dragstart`, `drop`, `input`, `keydown`, `keyup`, `mousedown`, `mousemove`, `mouseup`, `paste`, `pointercancel`, `pointerdown`, `pointermove`, `pointerup`, `touchcancel`, `touchend`, `touchmove`, `touchstart`, `wheel`.
- Emit `addEventListener` when a member call to `addEventListener` on the rooted path `addEventListener` with argument 0 matches one of the exact strings `beforeinput`, `compositionend`, `compositionstart`, `compositionupdate`, `contextmenu`, `copy`, `cut`, `dragend`, `dragover`, `dragstart`, `drop`, `input`, `keydown`, `keyup`, `mousedown`, `mousemove`, `mouseup`, `paste`, `pointercancel`, `pointerdown`, `pointermove`, `pointerup`, `touchcancel`, `touchend`, `touchmove`, `touchstart`, `wheel`.
- Emit `document.body.addEventListener` when a member call to `document.body.addEventListener` on the rooted path `document.body.addEventListener` with argument 0 matches one of the exact strings `beforeinput`, `compositionend`, `compositionstart`, `compositionupdate`, `contextmenu`, `copy`, `cut`, `dragend`, `dragover`, `dragstart`, `drop`, `input`, `keydown`, `keyup`, `mousedown`, `mousemove`, `mouseup`, `paste`, `pointercancel`, `pointerdown`, `pointermove`, `pointerup`, `touchcancel`, `touchend`, `touchmove`, `touchstart`, `wheel`.
- Emit `document.onkeydown` when a property write to `document.onkeydown` on the rooted path `document.onkeydown`.
- Emit `document.onkeyup` when a property write to `document.onkeyup` on the rooted path `document.onkeyup`.
- Emit `document.onkeypress` when a property write to `document.onkeypress` on the rooted path `document.onkeypress`.
- Emit `document.onpaste` when a property write to `document.onpaste` on the rooted path `document.onpaste`.
- Emit `document.oncopy` when a property write to `document.oncopy` on the rooted path `document.oncopy`.
- Emit `document.oncut` when a property write to `document.oncut` on the rooted path `document.oncut`.
- Emit `window.onkeydown` when a property write to `window.onkeydown` on the rooted path `window.onkeydown`.
- Emit `window.onkeyup` when a property write to `window.onkeyup` on the rooted path `window.onkeyup`.
- Emit `window.onpaste` when a property write to `window.onpaste` on the rooted path `window.onpaste`.
- Emit `window.oncopy` when a property write to `window.oncopy` on the rooted path `window.oncopy`.
- Emit `window.oncut` when a property write to `window.oncut` on the rooted path `window.oncut`.
- Emit `self.onkeydown` when a property write to `self.onkeydown` on the rooted path `self.onkeydown`.
- Emit `self.onkeyup` when a property write to `self.onkeyup` on the rooted path `self.onkeyup`.
- Emit `self.onpaste` when a property write to `self.onpaste` on the rooted path `self.onpaste`.
- Emit `globalThis.onkeydown` when a property write to `globalThis.onkeydown` on the rooted path `globalThis.onkeydown`.
- Emit `globalThis.onkeyup` when a property write to `globalThis.onkeyup` on the rooted path `globalThis.onkeyup`.
- Emit `document.body.onkeydown` when a property write to `document.body.onkeydown` on the rooted path `document.body.onkeydown`.
- Emit `document.body.onkeyup` when a property write to `document.body.onkeyup` on the rooted path `document.body.onkeyup`.

#### `browser:browser.file-dialog`

Default severity: `info`

Queries:

- Emit `file input element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `input`; it requires any of a write to `type` matching one of the exact strings `file`; a member call to `setAttribute` with argument 0 matches one of the exact strings `type` and argument 1 matches one of the exact strings `file`; it completes when the configuration condition.
- Emit `showOpenFilePicker` when a member call to `showOpenFilePicker` on the rooted path `showOpenFilePicker`.
- Emit `showSaveFilePicker` when a member call to `showSaveFilePicker` on the rooted path `showSaveFilePicker`.

#### `browser:browser.filesystem`

Default severity: `warning`

Queries:

- Emit `showDirectoryPicker` when a member call to `showDirectoryPicker` on the rooted path `showDirectoryPicker`.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `getFileHandle`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `getDirectoryHandle`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `removeEntry`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `resolve`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `queryPermission`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `requestPermission`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `entries`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `keys`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `values`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker` when all of: event $0 is selected; event $0 is a member call to `isSameEntry`; event $0 has identity the rooted path `showDirectoryPicker`; object $1 is returned by the rooted path `showDirectoryPicker`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker.getFileHandle` when all of: event $0 is selected; event $0 is a member call to `getFile`; event $0 has identity the rooted path `showDirectoryPicker.getFileHandle`; object $1 is returned by the rooted path `showDirectoryPicker.getFileHandle`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker.getFileHandle` when all of: event $0 is selected; event $0 is a member call to `createWritable`; event $0 has identity the rooted path `showDirectoryPicker.getFileHandle`; object $1 is returned by the rooted path `showDirectoryPicker.getFileHandle`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker.getFileHandle.createWritable` when all of: event $0 is selected; event $0 is a member call to `write`; event $0 has identity the rooted path `showDirectoryPicker.getFileHandle.createWritable`; object $1 is returned by the rooted path `showDirectoryPicker.getFileHandle.createWritable`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker.getFileHandle.createWritable` when all of: event $0 is selected; event $0 is a member call to `seek`; event $0 has identity the rooted path `showDirectoryPicker.getFileHandle.createWritable`; object $1 is returned by the rooted path `showDirectoryPicker.getFileHandle.createWritable`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker.getFileHandle.createWritable` when all of: event $0 is selected; event $0 is a member call to `truncate`; event $0 has identity the rooted path `showDirectoryPicker.getFileHandle.createWritable`; object $1 is returned by the rooted path `showDirectoryPicker.getFileHandle.createWritable`; event $0 uses object $1 as its member receiver.
- Emit `showDirectoryPicker.getFileHandle.createWritable` when all of: event $0 is selected; event $0 is a member call to `close`; event $0 has identity the rooted path `showDirectoryPicker.getFileHandle.createWritable`; object $1 is returned by the rooted path `showDirectoryPicker.getFileHandle.createWritable`; event $0 uses object $1 as its member receiver.

#### `browser:network.request`

Default severity: `info`

Queries:

- Emit `fetch` when a call to the global `fetch`.
- Emit `navigator.sendBeacon` when a member call to `navigator.sendBeacon` on the rooted path `navigator.sendBeacon`.
- Emit `XMLHttpRequest` when a constructor call to the global `XMLHttpRequest`.
- Emit `WebSocket` when a constructor call to the global `WebSocket`.
- Emit `EventSource` when a constructor call to the global `EventSource`.

#### `browser:dom.remote-resource`

Default severity: `warning`

Queries:

- Emit `remote script element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `script`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote image element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `img`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote link element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `link`; it requires any of a write to `href` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `href` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote iframe element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `iframe`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote audio element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `audio`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote video element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `video`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote source element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `source`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote object element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `object`; it requires any of a write to `data` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `data` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.
- Emit `remote embed element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `embed`; it requires any of a write to `src` matching a string starting with one of `//`, `http://`, `https://`; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches a string starting with one of `//`, `http://`, `https://`; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0, any argument of `document.body.append`, any argument of `document.body.prepend`, any argument of `document.documentElement.append`, any argument of `document.documentElement.prepend`, any argument of `document.head.append` receives the object.

#### `browser:dynamic-code.script-injection`

Default severity: `warning`

Queries:

- Emit `script-element` when a lifecycle object produced by a member call to `document.createElement` on the rooted path `document.createElement` with argument 0 matches one of the exact strings `script`; it requires any of a write to `src` matching any static string; a write to `text` matching any static string; a write to `textContent` matching any static string; a member call to `setAttribute` with argument 0 matches one of the exact strings `src` and argument 1 matches any static string; it completes when any sink `document.body.appendChild` argument 0, `document.documentElement.appendChild` argument 0, `document.documentElement.insertBefore` argument 0, `document.head.appendChild` argument 0 receives the object.
- Emit `document.write` when a member call to `document.write` on the rooted path `document.write` with argument 0 matches a string containing any of `<script`, `javascript:`.
- Emit `document.writeln` when a member call to `document.writeln` on the rooted path `document.writeln` with argument 0 matches a string containing any of `<script`, `javascript:`.
- Emit `document.body.insertAdjacentHTML` when a member call to `document.body.insertAdjacentHTML` on the rooted path `document.body.insertAdjacentHTML` with argument 1 matches a string containing any of `<script`, `javascript:`.
- Emit `document.documentElement.insertAdjacentHTML` when a member call to `document.documentElement.insertAdjacentHTML` on the rooted path `document.documentElement.insertAdjacentHTML` with argument 1 matches a string containing any of `<script`, `javascript:`.

### `node` catalog (6 rules)

#### `node:node.network`

Default severity: `info`

Queries:

- Emit `http` when an import of a string matching `http`.
- Emit `https` when an import of a string matching `https`.
- Emit `node:http` when an import of a string matching `node:http`.
- Emit `node:https` when an import of a string matching `node:https`.
- Emit `http2` when an import of a string matching `http2`.
- Emit `node:http2` when an import of a string matching `node:http2`.
- Emit `net` when an import of a string matching `net`.
- Emit `node:net` when an import of a string matching `node:net`.
- Emit `tls` when an import of a string matching `tls`.
- Emit `node:tls` when an import of a string matching `node:tls`.
- Emit `dgram` when an import of a string matching `dgram`.
- Emit `node:dgram` when an import of a string matching `node:dgram`.
- Emit `dns` when an import of a string matching `dns`.
- Emit `node:dns` when an import of a string matching `node:dns`.
- Emit `dns/promises` when an import of a string matching `dns/promises`.
- Emit `node:dns/promises` when an import of a string matching `node:dns/promises`.
- Emit `undici` when an import of the package specifier `undici`.
- Emit `axios` when an import of the package specifier `axios`.
- Emit `node-fetch` when an import of the package specifier `node-fetch`.
- Emit `got` when an import of the package specifier `got`.
- Emit `superagent` when an import of the package specifier `superagent`.
- Emit `ws` when an import of the package specifier `ws`.
- Emit `cross-fetch` when an import of the package specifier `cross-fetch`.
- Emit `ky` when an import of the package specifier `ky`.
- Emit `graphql-request` when an import of the package specifier `graphql-request`.
- Emit `request` when an import of the package specifier `request`.
- Emit `needle` when an import of the package specifier `needle`.
- Emit `@grpc/grpc-js` when an import of the package specifier `@grpc/grpc-js`.
- Emit `@apollo/client` when an import of the package specifier `@apollo/client`.
- Emit `graphql` when an import of the package specifier `graphql`.
- Emit `@elastic/elasticsearch` when an import of the package specifier `@elastic/elasticsearch`.
- Emit `fetch-retry` when an import of the package specifier `fetch-retry`.
- Emit `fetch-blob` when an import of the package specifier `fetch-blob`.
- Emit `form-data` when an import of the package specifier `form-data`.
- Emit `http-proxy` when an import of the package specifier `http-proxy`.
- Emit `http-proxy-agent` when an import of the package specifier `http-proxy-agent`.
- Emit `https-proxy-agent` when an import of the package specifier `https-proxy-agent`.
- Emit `socks-proxy-agent` when an import of the package specifier `socks-proxy-agent`.
- Emit `@whatwg-node/fetch` when an import of the package specifier `@whatwg-node/fetch`.

#### `node:node.filesystem`

Default severity: `info`

Queries:

- Emit `fs` when an import of a string matching `fs`.
- Emit `fs/promises` when an import of a string matching `fs/promises`.
- Emit `node:fs` when an import of a string matching `node:fs`.
- Emit `node:fs/promises` when an import of a string matching `node:fs/promises`.
- Emit `fs-extra` when an import of the package specifier `fs-extra`.
- Emit `graceful-fs` when an import of the package specifier `graceful-fs`.
- Emit `memfs` when an import of the package specifier `memfs`.
- Emit `unionfs` when an import of the package specifier `unionfs`.
- Emit `chokidar` when an import of the package specifier `chokidar`.
- Emit `proper-lockfile` when an import of the package specifier `proper-lockfile`.
- Emit `tmp` when an import of the package specifier `tmp`.
- Emit `tmp-promise` when an import of the package specifier `tmp-promise`.
- Emit `rimraf` when an import of the package specifier `rimraf`.
- Emit `mkdirp` when an import of the package specifier `mkdirp`.
- Emit `make-dir` when an import of the package specifier `make-dir`.
- Emit `write-file-atomic` when an import of the package specifier `write-file-atomic`.
- Emit `fs-monkey` when an import of the package specifier `fs-monkey`.
- Emit `mock-fs` when an import of the package specifier `mock-fs`.
- Emit `watchpack` when an import of the package specifier `watchpack`.
- Emit `fsevents` when an import of the package specifier `fsevents`.
- Emit `fs` when a member call to `readFile` on the namespace imported from module `fs`.
- Emit `fs.readFile` when a call to the `readFile` export from module `fs`.
- Emit `fs` when a member call to `readFileSync` on the namespace imported from module `fs`.
- Emit `fs.readFileSync` when a call to the `readFileSync` export from module `fs`.
- Emit `fs` when a member call to `writeFile` on the namespace imported from module `fs`.
- Emit `fs.writeFile` when a call to the `writeFile` export from module `fs`.
- Emit `fs` when a member call to `writeFileSync` on the namespace imported from module `fs`.
- Emit `fs.writeFileSync` when a call to the `writeFileSync` export from module `fs`.
- Emit `fs` when a member call to `appendFile` on the namespace imported from module `fs`.
- Emit `fs.appendFile` when a call to the `appendFile` export from module `fs`.
- Emit `fs` when a member call to `appendFileSync` on the namespace imported from module `fs`.
- Emit `fs.appendFileSync` when a call to the `appendFileSync` export from module `fs`.
- Emit `fs` when a member call to `mkdir` on the namespace imported from module `fs`.
- Emit `fs.mkdir` when a call to the `mkdir` export from module `fs`.
- Emit `fs` when a member call to `mkdirSync` on the namespace imported from module `fs`.
- Emit `fs.mkdirSync` when a call to the `mkdirSync` export from module `fs`.
- Emit `fs` when a member call to `rm` on the namespace imported from module `fs`.
- Emit `fs.rm` when a call to the `rm` export from module `fs`.
- Emit `fs` when a member call to `rmSync` on the namespace imported from module `fs`.
- Emit `fs.rmSync` when a call to the `rmSync` export from module `fs`.
- Emit `fs` when a member call to `unlink` on the namespace imported from module `fs`.
- Emit `fs.unlink` when a call to the `unlink` export from module `fs`.
- Emit `fs` when a member call to `unlinkSync` on the namespace imported from module `fs`.
- Emit `fs.unlinkSync` when a call to the `unlinkSync` export from module `fs`.
- Emit `fs` when a member call to `rename` on the namespace imported from module `fs`.
- Emit `fs.rename` when a call to the `rename` export from module `fs`.
- Emit `fs` when a member call to `renameSync` on the namespace imported from module `fs`.
- Emit `fs.renameSync` when a call to the `renameSync` export from module `fs`.
- Emit `fs` when a member call to `copyFile` on the namespace imported from module `fs`.
- Emit `fs.copyFile` when a call to the `copyFile` export from module `fs`.
- Emit `fs` when a member call to `copyFileSync` on the namespace imported from module `fs`.
- Emit `fs.copyFileSync` when a call to the `copyFileSync` export from module `fs`.
- Emit `fs` when a member call to `readdir` on the namespace imported from module `fs`.
- Emit `fs.readdir` when a call to the `readdir` export from module `fs`.
- Emit `fs` when a member call to `readdirSync` on the namespace imported from module `fs`.
- Emit `fs.readdirSync` when a call to the `readdirSync` export from module `fs`.
- Emit `fs` when a member call to `stat` on the namespace imported from module `fs`.
- Emit `fs.stat` when a call to the `stat` export from module `fs`.
- Emit `fs` when a member call to `statSync` on the namespace imported from module `fs`.
- Emit `fs.statSync` when a call to the `statSync` export from module `fs`.
- Emit `fs` when a member call to `open` on the namespace imported from module `fs`.
- Emit `fs.open` when a call to the `open` export from module `fs`.
- Emit `fs` when a member call to `openSync` on the namespace imported from module `fs`.
- Emit `fs.openSync` when a call to the `openSync` export from module `fs`.
- Emit `fs` when a member call to `watch` on the namespace imported from module `fs`.
- Emit `fs.watch` when a call to the `watch` export from module `fs`.
- Emit `fs` when a member call to `watchFile` on the namespace imported from module `fs`.
- Emit `fs.watchFile` when a call to the `watchFile` export from module `fs`.
- Emit `fs/promises` when a member call to `readFile` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.readFile` when a call to the `readFile` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `readFileSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.readFileSync` when a call to the `readFileSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `writeFile` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.writeFile` when a call to the `writeFile` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `writeFileSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.writeFileSync` when a call to the `writeFileSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `appendFile` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.appendFile` when a call to the `appendFile` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `appendFileSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.appendFileSync` when a call to the `appendFileSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `mkdir` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.mkdir` when a call to the `mkdir` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `mkdirSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.mkdirSync` when a call to the `mkdirSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `rm` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.rm` when a call to the `rm` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `rmSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.rmSync` when a call to the `rmSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `unlink` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.unlink` when a call to the `unlink` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `unlinkSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.unlinkSync` when a call to the `unlinkSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `rename` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.rename` when a call to the `rename` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `renameSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.renameSync` when a call to the `renameSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `copyFile` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.copyFile` when a call to the `copyFile` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `copyFileSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.copyFileSync` when a call to the `copyFileSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `readdir` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.readdir` when a call to the `readdir` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `readdirSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.readdirSync` when a call to the `readdirSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `stat` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.stat` when a call to the `stat` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `statSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.statSync` when a call to the `statSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `open` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.open` when a call to the `open` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `openSync` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.openSync` when a call to the `openSync` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `watch` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.watch` when a call to the `watch` export from module `fs/promises`.
- Emit `fs/promises` when a member call to `watchFile` on the namespace imported from module `fs/promises`.
- Emit `fs/promises.watchFile` when a call to the `watchFile` export from module `fs/promises`.
- Emit `node:fs` when a member call to `readFile` on the namespace imported from module `node:fs`.
- Emit `node:fs.readFile` when a call to the `readFile` export from module `node:fs`.
- Emit `node:fs` when a member call to `readFileSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.readFileSync` when a call to the `readFileSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `writeFile` on the namespace imported from module `node:fs`.
- Emit `node:fs.writeFile` when a call to the `writeFile` export from module `node:fs`.
- Emit `node:fs` when a member call to `writeFileSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.writeFileSync` when a call to the `writeFileSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `appendFile` on the namespace imported from module `node:fs`.
- Emit `node:fs.appendFile` when a call to the `appendFile` export from module `node:fs`.
- Emit `node:fs` when a member call to `appendFileSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.appendFileSync` when a call to the `appendFileSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `mkdir` on the namespace imported from module `node:fs`.
- Emit `node:fs.mkdir` when a call to the `mkdir` export from module `node:fs`.
- Emit `node:fs` when a member call to `mkdirSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.mkdirSync` when a call to the `mkdirSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `rm` on the namespace imported from module `node:fs`.
- Emit `node:fs.rm` when a call to the `rm` export from module `node:fs`.
- Emit `node:fs` when a member call to `rmSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.rmSync` when a call to the `rmSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `unlink` on the namespace imported from module `node:fs`.
- Emit `node:fs.unlink` when a call to the `unlink` export from module `node:fs`.
- Emit `node:fs` when a member call to `unlinkSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.unlinkSync` when a call to the `unlinkSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `rename` on the namespace imported from module `node:fs`.
- Emit `node:fs.rename` when a call to the `rename` export from module `node:fs`.
- Emit `node:fs` when a member call to `renameSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.renameSync` when a call to the `renameSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `copyFile` on the namespace imported from module `node:fs`.
- Emit `node:fs.copyFile` when a call to the `copyFile` export from module `node:fs`.
- Emit `node:fs` when a member call to `copyFileSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.copyFileSync` when a call to the `copyFileSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `readdir` on the namespace imported from module `node:fs`.
- Emit `node:fs.readdir` when a call to the `readdir` export from module `node:fs`.
- Emit `node:fs` when a member call to `readdirSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.readdirSync` when a call to the `readdirSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `stat` on the namespace imported from module `node:fs`.
- Emit `node:fs.stat` when a call to the `stat` export from module `node:fs`.
- Emit `node:fs` when a member call to `statSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.statSync` when a call to the `statSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `open` on the namespace imported from module `node:fs`.
- Emit `node:fs.open` when a call to the `open` export from module `node:fs`.
- Emit `node:fs` when a member call to `openSync` on the namespace imported from module `node:fs`.
- Emit `node:fs.openSync` when a call to the `openSync` export from module `node:fs`.
- Emit `node:fs` when a member call to `watch` on the namespace imported from module `node:fs`.
- Emit `node:fs.watch` when a call to the `watch` export from module `node:fs`.
- Emit `node:fs` when a member call to `watchFile` on the namespace imported from module `node:fs`.
- Emit `node:fs.watchFile` when a call to the `watchFile` export from module `node:fs`.
- Emit `node:fs/promises` when a member call to `readFile` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.readFile` when a call to the `readFile` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `readFileSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.readFileSync` when a call to the `readFileSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `writeFile` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.writeFile` when a call to the `writeFile` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `writeFileSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.writeFileSync` when a call to the `writeFileSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `appendFile` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.appendFile` when a call to the `appendFile` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `appendFileSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.appendFileSync` when a call to the `appendFileSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `mkdir` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.mkdir` when a call to the `mkdir` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `mkdirSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.mkdirSync` when a call to the `mkdirSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `rm` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.rm` when a call to the `rm` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `rmSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.rmSync` when a call to the `rmSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `unlink` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.unlink` when a call to the `unlink` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `unlinkSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.unlinkSync` when a call to the `unlinkSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `rename` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.rename` when a call to the `rename` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `renameSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.renameSync` when a call to the `renameSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `copyFile` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.copyFile` when a call to the `copyFile` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `copyFileSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.copyFileSync` when a call to the `copyFileSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `readdir` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.readdir` when a call to the `readdir` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `readdirSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.readdirSync` when a call to the `readdirSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `stat` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.stat` when a call to the `stat` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `statSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.statSync` when a call to the `statSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `open` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.open` when a call to the `open` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `openSync` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.openSync` when a call to the `openSync` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `watch` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.watch` when a call to the `watch` export from module `node:fs/promises`.
- Emit `node:fs/promises` when a member call to `watchFile` on the namespace imported from module `node:fs/promises`.
- Emit `node:fs/promises.watchFile` when a call to the `watchFile` export from module `node:fs/promises`.

#### `node:node.process-environment`

Default severity: `info`

Queries:

- Emit `process.env` when a member read of `process.env` on the rooted path `process.env`.
- Emit `process.platform` when a member read of `process.platform` on the rooted path `process.platform`.
- Emit `process.argv` when a member read of `process.argv` on the rooted path `process.argv`.
- Emit `process.execPath` when a member read of `process.execPath` on the rooted path `process.execPath`.
- Emit `process.arch` when a member read of `process.arch` on the rooted path `process.arch`.
- Emit `process.version` when a member read of `process.version` on the rooted path `process.version`.
- Emit `process.versions` when a member read of `process.versions` on the rooted path `process.versions`.
- Emit `process.release` when a member read of `process.release` on the rooted path `process.release`.
- Emit `process.pid` when a member read of `process.pid` on the rooted path `process.pid`.
- Emit `process.ppid` when a member read of `process.ppid` on the rooted path `process.ppid`.
- Emit `process.execArgv` when a member read of `process.execArgv` on the rooted path `process.execArgv`.
- Emit `process.title` when a member read of `process.title` on the rooted path `process.title`.
- Emit `process.config` when a member read of `process.config` on the rooted path `process.config`.
- Emit `process.features` when a member read of `process.features` on the rooted path `process.features`.
- Emit `process.report` when a member read of `process.report` on the rooted path `process.report`.
- Emit `process.allowedNodeEnvironmentFlags` when a member read of `process.allowedNodeEnvironmentFlags` on the rooted path `process.allowedNodeEnvironmentFlags`.
- Emit `process.debugPort` when a member read of `process.debugPort` on the rooted path `process.debugPort`.
- Emit `process.sourceMapsEnabled` when a member read of `process.sourceMapsEnabled` on the rooted path `process.sourceMapsEnabled`.
- Emit `process.cwd` when a member call to `process.cwd` on the rooted path `process.cwd`.
- Emit `process.memoryUsage` when a member call to `process.memoryUsage` on the rooted path `process.memoryUsage`.
- Emit `process.resourceUsage` when a member call to `process.resourceUsage` on the rooted path `process.resourceUsage`.
- Emit `process.cpuUsage` when a member call to `process.cpuUsage` on the rooted path `process.cpuUsage`.
- Emit `process.uptime` when a member call to `process.uptime` on the rooted path `process.uptime`.
- Emit `process.hrtime` when a member call to `process.hrtime` on the rooted path `process.hrtime`.
- Emit `process.getActiveResourcesInfo` when a member call to `process.getActiveResourcesInfo` on the rooted path `process.getActiveResourcesInfo`.
- Emit `process.constrainedMemory` when a member call to `process.constrainedMemory` on the rooted path `process.constrainedMemory`.
- Emit `process.getuid` when a member call to `process.getuid` on the rooted path `process.getuid`.
- Emit `process.geteuid` when a member call to `process.geteuid` on the rooted path `process.geteuid`.
- Emit `process.getgid` when a member call to `process.getgid` on the rooted path `process.getgid`.
- Emit `process.getegid` when a member call to `process.getegid` on the rooted path `process.getegid`.
- Emit `process.getgroups` when a member call to `process.getgroups` on the rooted path `process.getgroups`.
- Emit `process.umask` when a member call to `process.umask` on the rooted path `process.umask`.
- Emit `process.getBuiltinModule` when a member call to `process.getBuiltinModule` on the rooted path `process.getBuiltinModule`.
- Emit `process.loadEnvFile` when a member call to `process.loadEnvFile` on the rooted path `process.loadEnvFile`.

#### `node:node.subprocess`

Default severity: `warning`

Queries:

- Emit `child_process` when an import of a string matching `child_process`.
- Emit `node:child_process` when an import of a string matching `node:child_process`.
- Emit `worker_threads` when an import of a string matching `worker_threads`.
- Emit `node:worker_threads` when an import of a string matching `node:worker_threads`.
- Emit `cluster` when an import of a string matching `cluster`.
- Emit `node:cluster` when an import of a string matching `node:cluster`.
- Emit `node-pty` when an import of the package specifier `node-pty`.
- Emit `pty.js` when an import of the package specifier `pty.js`.
- Emit `execa` when an import of the package specifier `execa`.
- Emit `cross-spawn` when an import of the package specifier `cross-spawn`.
- Emit `shelljs` when an import of the package specifier `shelljs`.
- Emit `zx` when an import of the package specifier `zx`.
- Emit `npm-run-path` when an import of the package specifier `npm-run-path`.
- Emit `foreground-child` when an import of the package specifier `foreground-child`.
- Emit `spawn-command` when an import of the package specifier `spawn-command`.
- Emit `concurrently` when an import of the package specifier `concurrently`.
- Emit `npm-run-all` when an import of the package specifier `npm-run-all`.
- Emit `sudo-prompt` when an import of the package specifier `sudo-prompt`.
- Emit `child_process` when a member call to `spawn` on the namespace imported from module `child_process`.
- Emit `child_process` when a member call to `exec` on the namespace imported from module `child_process`.
- Emit `child_process` when a member call to `execFile` on the namespace imported from module `child_process`.
- Emit `child_process` when a member call to `fork` on the namespace imported from module `child_process`.
- Emit `node:child_process` when a member call to `spawn` on the namespace imported from module `node:child_process`.
- Emit `node:child_process` when a member call to `exec` on the namespace imported from module `node:child_process`.
- Emit `node:child_process` when a member call to `execFile` on the namespace imported from module `node:child_process`.
- Emit `node:child_process` when a member call to `fork` on the namespace imported from module `node:child_process`.
- Emit `cluster` when a member call to `fork` on the namespace imported from module `cluster`.
- Emit `node:cluster` when a member call to `fork` on the namespace imported from module `node:cluster`.
- Emit `Worker` when a constructor call to the global `Worker`.

#### `node:archive.compression`

Default severity: `info`

Queries:

- Emit `jszip` when an import of the package specifier `jszip`.
- Emit `tar` when an import of the package specifier `tar`.
- Emit `zlib` when an import of the package specifier `zlib`.
- Emit `node:zlib` when an import of a string matching `node:zlib`.
- Emit `fflate` when an import of the package specifier `fflate`.
- Emit `archiver` when an import of the package specifier `archiver`.
- Emit `yauzl` when an import of the package specifier `yauzl`.
- Emit `unzipper` when an import of the package specifier `unzipper`.
- Emit `node-tar` when an import of the package specifier `node-tar`.
- Emit `compressing` when an import of the package specifier `compressing`.
- Emit `adm-zip` when an import of the package specifier `adm-zip`.
- Emit `extract-zip` when an import of the package specifier `extract-zip`.
- Emit `tar-stream` when an import of the package specifier `tar-stream`.
- Emit `pako` when an import of the package specifier `pako`.
- Emit `decompress` when an import of the package specifier `decompress`.
- Emit `zip-a-folder` when an import of the package specifier `zip-a-folder`.
- Emit `@zip.js/zip.js` when an import of the package specifier `@zip.js/zip.js`.
- Emit `yazl` when an import of the package specifier `yazl`.
- Emit `node-stream-zip` when an import of the package specifier `node-stream-zip`.

#### `node:crypto.operation`

Default severity: `info`

Queries:

- Emit `crypto` when an import of a string matching `crypto`.
- Emit `crypto/promises` when an import of a string matching `crypto/promises`.
- Emit `node:crypto` when an import of a string matching `node:crypto`.
- Emit `node:crypto/promises` when an import of a string matching `node:crypto/promises`.
- Emit `crypto-js` when an import of the package specifier `crypto-js`.
- Emit `@noble/hashes` when an import of the package specifier `@noble/hashes`.
- Emit `@noble/curves` when an import of the package specifier `@noble/curves`.
- Emit `tweetnacl` when an import of the package specifier `tweetnacl`.
- Emit `libsodium-wrappers` when an import of the package specifier `libsodium-wrappers`.
- Emit `jose` when an import of the package specifier `jose`.
- Emit `jsonwebtoken` when an import of the package specifier `jsonwebtoken`.
- Emit `node-forge` when an import of the package specifier `node-forge`.
- Emit `elliptic` when an import of the package specifier `elliptic`.
- Emit `bcrypt` when an import of the package specifier `bcrypt`.
- Emit `bcryptjs` when an import of the package specifier `bcryptjs`.
- Emit `argon2` when an import of the package specifier `argon2`.
- Emit `scrypt-js` when an import of the package specifier `scrypt-js`.
- Emit `crypto.subtle.digest` when a member call to `crypto.subtle.digest` on the rooted path `crypto.subtle.digest`.
- Emit `crypto.subtle.encrypt` when a member call to `crypto.subtle.encrypt` on the rooted path `crypto.subtle.encrypt`.
- Emit `crypto.subtle.decrypt` when a member call to `crypto.subtle.decrypt` on the rooted path `crypto.subtle.decrypt`.
- Emit `crypto.subtle.sign` when a member call to `crypto.subtle.sign` on the rooted path `crypto.subtle.sign`.
- Emit `crypto.subtle.verify` when a member call to `crypto.subtle.verify` on the rooted path `crypto.subtle.verify`.
- Emit `crypto.subtle.deriveBits` when a member call to `crypto.subtle.deriveBits` on the rooted path `crypto.subtle.deriveBits`.
- Emit `crypto.subtle.deriveKey` when a member call to `crypto.subtle.deriveKey` on the rooted path `crypto.subtle.deriveKey`.
- Emit `crypto.subtle.generateKey` when a member call to `crypto.subtle.generateKey` on the rooted path `crypto.subtle.generateKey`.
- Emit `crypto.subtle.importKey` when a member call to `crypto.subtle.importKey` on the rooted path `crypto.subtle.importKey`.
- Emit `crypto.subtle.exportKey` when a member call to `crypto.subtle.exportKey` on the rooted path `crypto.subtle.exportKey`.
- Emit `crypto.subtle.wrapKey` when a member call to `crypto.subtle.wrapKey` on the rooted path `crypto.subtle.wrapKey`.
- Emit `crypto.subtle.unwrapKey` when a member call to `crypto.subtle.unwrapKey` on the rooted path `crypto.subtle.unwrapKey`.
- Emit `globalThis.crypto.subtle.digest` when a member call to `globalThis.crypto.subtle.digest` on the rooted path `globalThis.crypto.subtle.digest`.
- Emit `globalThis.crypto.subtle.encrypt` when a member call to `globalThis.crypto.subtle.encrypt` on the rooted path `globalThis.crypto.subtle.encrypt`.
- Emit `globalThis.crypto.subtle.decrypt` when a member call to `globalThis.crypto.subtle.decrypt` on the rooted path `globalThis.crypto.subtle.decrypt`.
- Emit `webcrypto.subtle.digest` when a member call to `webcrypto.subtle.digest` on the rooted path `webcrypto.subtle.digest`.
- Emit `webcrypto.subtle.encrypt` when a member call to `webcrypto.subtle.encrypt` on the rooted path `webcrypto.subtle.encrypt`.
- Emit `webcrypto.subtle.decrypt` when a member call to `webcrypto.subtle.decrypt` on the rooted path `webcrypto.subtle.decrypt`.
- Emit `crypto` when a member call to `createHash` on the namespace imported from module `crypto`.
- Emit `node:crypto` when a member call to `createHash` on the namespace imported from module `node:crypto`.
- Emit `crypto` when a member call to `createCipheriv` on the namespace imported from module `crypto`.
- Emit `node:crypto` when a member call to `createCipheriv` on the namespace imported from module `node:crypto`.
- Emit `crypto` when a member call to `generateKeyPair` on the namespace imported from module `crypto`.
- Emit `node:crypto` when a member call to `generateKeyPair` on the namespace imported from module `node:crypto`.
- Emit `crypto` when a member call to `sign` on the namespace imported from module `crypto`.
- Emit `node:crypto` when a member call to `sign` on the namespace imported from module `node:crypto`.

### `electron` catalog (4 rules)

#### `electron:electron.module`

Default severity: `info`

Queries:

- Emit `electron` when an import of a string matching `electron`.
- Emit `electron/main` when an import of a string matching `electron/main`.
- Emit `electron/renderer` when an import of a string matching `electron/renderer`.
- Emit `electron/common` when an import of a string matching `electron/common`.
- Emit `electron/utility` when an import of a string matching `electron/utility`.
- Emit `electron/sandbox` when an import of a string matching `electron/sandbox`.
- Emit `electron.BrowserWindow` when a constructor call to the `BrowserWindow` export from module `electron`.
- Emit `electron` when a member call to `webContents.fromId` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.fromFrame` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.getAllWebContents` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.getFocusedWebContents` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `session.fromPartition` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `contextBridge.exposeInMainWorld` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `contextBridge.exposeInIsolatedWorld` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `globalShortcut.register` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `globalShortcut.unregister` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `globalShortcut.registerAll` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `globalShortcut.unregisterAll` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `globalShortcut.isRegistered` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `desktopCapturer.getSources` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `powerMonitor.getSystemIdleState` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `powerMonitor.getSystemIdleTime` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `powerMonitor.getSystemMemoryInfo` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `powerMonitor.getCPUUsage` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `powerMonitor.getIOCounters` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.getPath` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.getVersion` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.getAppPath` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.getName` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.getLocale` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.quit` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `app.whenReady` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.readText` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.writeText` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.clear` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.readHTML` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.readImage` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.writeHTML` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.writeImage` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `clipboard.availableFormats` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `safeStorage.encryptString` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `safeStorage.decryptString` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `safeStorage.isEncryptionAvailable` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `screen.getAllDisplays` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `screen.getPrimaryDisplay` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `screen.getCursorScreenPoint` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `screen.getDisplayNearestPoint` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `screen.getDisplayMatching` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `protocol.registerFileProtocol` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `protocol.registerStringProtocol` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `protocol.registerHttpProtocol` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `protocol.registerBufferProtocol` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `protocol.unregisterProtocol` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `nativeImage.createFromPath` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `nativeImage.createFromBuffer` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `nativeImage.createFromDataURL` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `nativeImage.createEmpty` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `BrowserWindow.getAllWindows` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `BrowserWindow.getFocusedWindow` on the namespace imported from module `electron`.
- Emit `electron` when a member read of `nativeTheme.shouldUseDarkColors` on the namespace imported from module `electron`.
- Emit `electron` when a member read of `app.isPackaged` on the namespace imported from module `electron`.
- Emit `electron` when a member read of `session.defaultSession` on the namespace imported from module `electron`.

#### `electron:electron.ipc`

Default severity: `info`

Queries:

- Emit `electron` when a member call to `ipcRenderer.send` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.invoke` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.sendSync` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.postMessage` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.sendToHost` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.on` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.once` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.addListener` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.removeListener` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.off` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcRenderer.removeAllListeners` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.on` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.once` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.handle` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.handleOnce` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.removeHandler` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.removeListener` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.off` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `ipcMain.removeAllListeners` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.send` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.sendToFrame` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.postMessage` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.on` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.once` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.removeListener` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.off` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webContents.removeAllListeners` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.send` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.postMessage` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.on` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.once` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.removeListener` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.off` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `webFrameMain.removeAllListeners` on the namespace imported from module `electron`.

#### `electron:electron.shell`

Default severity: `warning`

Queries:

- Emit `electron` when a member call to `shell.openExternal` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `shell.openPath` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `shell.showItemInFolder` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `shell.trashItem` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `shell.beep` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `shell.readShortcutLink` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `shell.writeShortcutLink` on the namespace imported from module `electron`.

#### `electron:electron.dialog`

Default severity: `info`

Queries:

- Emit `electron` when a member call to `dialog.showOpenDialog` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showSaveDialog` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showOpenDialogSync` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showSaveDialogSync` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showMessageBox` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showMessageBoxSync` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showErrorBox` on the namespace imported from module `electron`.
- Emit `electron` when a member call to `dialog.showCertificateTrustDialog` on the namespace imported from module `electron`.

### `obsidian` catalog (49 rules)

#### `obsidian:network.request`

Default severity: `info`

Queries:

- Emit `request` when a call to the global `request`.
- Emit `requestUrl` when a call to the global `requestUrl`.
- Emit `obsidian` when a member call to `request` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `requestUrl` on the namespace imported from module `obsidian`.

#### `obsidian:vault.access`

Default severity: `info`

Queries:

- Emit `app.vault` when a member read of `app.vault` on the rooted path `app.vault`.

#### `obsidian:vault.read`

Default severity: `info`

Queries:

- Emit `app.vault.read` when a member call to `app.vault.read` on the rooted path `app.vault.read`.
- Emit `app.vault.cachedRead` when a member call to `app.vault.cachedRead` on the rooted path `app.vault.cachedRead`.
- Emit `app.vault.readBinary` when a member call to `app.vault.readBinary` on the rooted path `app.vault.readBinary`.

#### `obsidian:vault.write`

Default severity: `info`

Queries:

- Emit `app.vault.create` when a member call to `app.vault.create` on the rooted path `app.vault.create`.
- Emit `app.vault.createBinary` when a member call to `app.vault.createBinary` on the rooted path `app.vault.createBinary`.
- Emit `app.vault.modify` when a member call to `app.vault.modify` on the rooted path `app.vault.modify`.
- Emit `app.vault.modifyBinary` when a member call to `app.vault.modifyBinary` on the rooted path `app.vault.modifyBinary`.
- Emit `app.vault.append` when a member call to `app.vault.append` on the rooted path `app.vault.append`.
- Emit `app.vault.appendBinary` when a member call to `app.vault.appendBinary` on the rooted path `app.vault.appendBinary`.
- Emit `app.vault.process` when a member call to `app.vault.process` on the rooted path `app.vault.process`.
- Emit `app.vault.createFolder` when a member call to `app.vault.createFolder` on the rooted path `app.vault.createFolder`.

#### `obsidian:vault.delete`

Default severity: `warning`

Queries:

- Emit `app.vault.delete` when a member call to `app.vault.delete` on the rooted path `app.vault.delete`.
- Emit `app.vault.trash` when a member call to `app.vault.trash` on the rooted path `app.vault.trash`.
- Emit `app.fileManager.trashFile` when a member call to `app.fileManager.trashFile` on the rooted path `app.fileManager.trashFile`.

#### `obsidian:vault.move-copy`

Default severity: `info`

Queries:

- Emit `app.vault.rename` when a member call to `app.vault.rename` on the rooted path `app.vault.rename`.
- Emit `app.vault.copy` when a member call to `app.vault.copy` on the rooted path `app.vault.copy`.
- Emit `app.fileManager.renameFile` when a member call to `app.fileManager.renameFile` on the rooted path `app.fileManager.renameFile`.

#### `obsidian:vault.enumerate`

Default severity: `info`

Queries:

- Emit `app.vault.getFiles` when a member call to `app.vault.getFiles` on the rooted path `app.vault.getFiles`.
- Emit `app.vault.getMarkdownFiles` when a member call to `app.vault.getMarkdownFiles` on the rooted path `app.vault.getMarkdownFiles`.
- Emit `app.vault.getAllLoadedFiles` when a member call to `app.vault.getAllLoadedFiles` on the rooted path `app.vault.getAllLoadedFiles`.
- Emit `app.vault.getAllFolders` when a member call to `app.vault.getAllFolders` on the rooted path `app.vault.getAllFolders`.
- Emit `app.vault.getFolderByPath` when a member call to `app.vault.getFolderByPath` on the rooted path `app.vault.getFolderByPath`.
- Emit `app.vault.getFileByPath` when a member call to `app.vault.getFileByPath` on the rooted path `app.vault.getFileByPath`.
- Emit `app.vault.getAbstractFileByPath` when a member call to `app.vault.getAbstractFileByPath` on the rooted path `app.vault.getAbstractFileByPath`.
- Emit `Vault.recurseChildren` when a member call to `Vault.recurseChildren` on the rooted path `Vault.recurseChildren`.
- Emit `app.vault.getRoot` when a member call to `app.vault.getRoot` on the rooted path `app.vault.getRoot`.

#### `obsidian:vault.adapter`

Default severity: `info`

Queries:

- Emit `app.vault.adapter.exists` when a member call to `app.vault.adapter.exists` on the rooted path `app.vault.adapter.exists`.
- Emit `app.vault.adapter.stat` when a member call to `app.vault.adapter.stat` on the rooted path `app.vault.adapter.stat`.
- Emit `app.vault.adapter.list` when a member call to `app.vault.adapter.list` on the rooted path `app.vault.adapter.list`.
- Emit `app.vault.adapter.read` when a member call to `app.vault.adapter.read` on the rooted path `app.vault.adapter.read`.
- Emit `app.vault.adapter.write` when a member call to `app.vault.adapter.write` on the rooted path `app.vault.adapter.write`.
- Emit `app.vault.adapter.append` when a member call to `app.vault.adapter.append` on the rooted path `app.vault.adapter.append`.
- Emit `app.vault.adapter.process` when a member call to `app.vault.adapter.process` on the rooted path `app.vault.adapter.process`.
- Emit `app.vault.adapter.mkdir` when a member call to `app.vault.adapter.mkdir` on the rooted path `app.vault.adapter.mkdir`.
- Emit `app.vault.adapter.remove` when a member call to `app.vault.adapter.remove` on the rooted path `app.vault.adapter.remove`.
- Emit `app.vault.adapter.rename` when a member call to `app.vault.adapter.rename` on the rooted path `app.vault.adapter.rename`.
- Emit `app.vault.adapter.copy` when a member call to `app.vault.adapter.copy` on the rooted path `app.vault.adapter.copy`.
- Emit `app.vault.adapter.getFullPath` when a member call to `app.vault.adapter.getFullPath` on the rooted path `app.vault.adapter.getFullPath`.

#### `obsidian:vault.config-directory`

Default severity: `info`

Queries:

- Emit `.obsidian/` when a string reference containing `.obsidian/`.
- Emit `.obsidian\` when a string reference containing `.obsidian\`.
- Emit `app.vault.configDir` when a member read of `app.vault.configDir` on the rooted path `app.vault.configDir`.

#### `obsidian:vault.resource-url`

Default severity: `info`

Queries:

- Emit `app.vault.getResourcePath` when a member call to `app.vault.getResourcePath` on the rooted path `app.vault.getResourcePath`.
- Emit `app.vault.adapter.getResourcePath` when a member call to `app.vault.adapter.getResourcePath` on the rooted path `app.vault.adapter.getResourcePath`.
- Emit `obsidian://` when a string reference containing `obsidian://`.

#### `obsidian:vault.events`

Default severity: `info`

Queries:

- Emit `app.vault.on` when a member call to `app.vault.on` on the rooted path `app.vault.on` with argument 0 matches one of the exact strings `create`, `delete`, `modify`, `rename`.

#### `obsidian:metadata.cache-read`

Default severity: `info`

Queries:

- Emit `app.metadataCache` when a member read of `app.metadataCache` on the rooted path `app.metadataCache`.
- Emit `app.metadataCache.resolvedLinks` when a member read of `app.metadataCache.resolvedLinks` on the rooted path `app.metadataCache.resolvedLinks`.
- Emit `app.metadataCache.unresolvedLinks` when a member read of `app.metadataCache.unresolvedLinks` on the rooted path `app.metadataCache.unresolvedLinks`.
- Emit `app.metadataCache.getFileCache` when a member call to `app.metadataCache.getFileCache` on the rooted path `app.metadataCache.getFileCache`.
- Emit `app.metadataCache.getCache` when a member call to `app.metadataCache.getCache` on the rooted path `app.metadataCache.getCache`.
- Emit `app.metadataCache.getFirstLinkpathDest` when a member call to `app.metadataCache.getFirstLinkpathDest` on the rooted path `app.metadataCache.getFirstLinkpathDest`.

#### `obsidian:metadata.frontmatter-read`

Default severity: `info`

Queries:

- Emit `app.metadataCache.getFileCache.frontmatter` when a member read of `app.metadataCache.getFileCache.frontmatter` on the rooted path `app.metadataCache.getFileCache.frontmatter`.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `frontmatter`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `frontmatter`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `obsidian` when a member call to `parseFrontMatterAliases` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `parseFrontMatterTags` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `parseFrontMatterEntry` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `parseFrontMatterStringArray` on the namespace imported from module `obsidian`.

#### `obsidian:metadata.events`

Default severity: `info`

Queries:

- Emit `app.metadataCache.on` when a member call to `app.metadataCache.on` on the rooted path `app.metadataCache.on` with argument 0 matches one of the exact strings `changed`, `deleted`, `resolve`, `resolved`.

#### `obsidian:metadata.traversal`

Default severity: `info`

Queries:

- Emit `Object.entries` when a member call to `Object.entries` on the rooted path `Object.entries` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `Object.keys` when a member call to `Object.keys` on the rooted path `Object.keys` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `Object.values` when a member call to `Object.values` on the rooted path `Object.values` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `Object.getOwnPropertyNames` when a member call to `Object.getOwnPropertyNames` on the rooted path `Object.getOwnPropertyNames` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `Object.getOwnPropertySymbols` when a member call to `Object.getOwnPropertySymbols` on the rooted path `Object.getOwnPropertySymbols` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `Object.getOwnPropertyDescriptors` when a member call to `Object.getOwnPropertyDescriptors` on the rooted path `Object.getOwnPropertyDescriptors` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `Reflect.ownKeys` when a member call to `Reflect.ownKeys` on the rooted path `Reflect.ownKeys` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Object.keys` when a member call to `global.Object.keys` on the rooted path `global.Object.keys` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Object.entries` when a member call to `global.Object.entries` on the rooted path `global.Object.entries` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Object.values` when a member call to `global.Object.values` on the rooted path `global.Object.values` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Object.getOwnPropertyNames` when a member call to `global.Object.getOwnPropertyNames` on the rooted path `global.Object.getOwnPropertyNames` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Object.getOwnPropertySymbols` when a member call to `global.Object.getOwnPropertySymbols` on the rooted path `global.Object.getOwnPropertySymbols` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Object.getOwnPropertyDescriptors` when a member call to `global.Object.getOwnPropertyDescriptors` on the rooted path `global.Object.getOwnPropertyDescriptors` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.
- Emit `global.Reflect.ownKeys` when a member call to `global.Reflect.ownKeys` on the rooted path `global.Reflect.ownKeys` with argument 0 matches one of the rooted expressions `app.metadataCache.resolvedLinks`, `app.metadataCache.unresolvedLinks`.

#### `obsidian:metadata.extract`

Default severity: `info`

Queries:

- Emit `app.metadataCache.getFileCache.tags` when a member read of `app.metadataCache.getFileCache.tags` on the rooted path `app.metadataCache.getFileCache.tags`.
- Emit `app.metadataCache.getFileCache.links` when a member read of `app.metadataCache.getFileCache.links` on the rooted path `app.metadataCache.getFileCache.links`.
- Emit `app.metadataCache.getFileCache.embeds` when a member read of `app.metadataCache.getFileCache.embeds` on the rooted path `app.metadataCache.getFileCache.embeds`.
- Emit `app.metadataCache.getFileCache.blocks` when a member read of `app.metadataCache.getFileCache.blocks` on the rooted path `app.metadataCache.getFileCache.blocks`.
- Emit `app.metadataCache.getFileCache.headings` when a member read of `app.metadataCache.getFileCache.headings` on the rooted path `app.metadataCache.getFileCache.headings`.
- Emit `app.metadataCache.getFileCache.sections` when a member read of `app.metadataCache.getFileCache.sections` on the rooted path `app.metadataCache.getFileCache.sections`.
- Emit `app.metadataCache.getFileCache.listItems` when a member read of `app.metadataCache.getFileCache.listItems` on the rooted path `app.metadataCache.getFileCache.listItems`.
- Emit `app.metadataCache.getFileCache.footnotes` when a member read of `app.metadataCache.getFileCache.footnotes` on the rooted path `app.metadataCache.getFileCache.footnotes`.
- Emit `app.metadataCache.getFileCache.footnoteRefs` when a member read of `app.metadataCache.getFileCache.footnoteRefs` on the rooted path `app.metadataCache.getFileCache.footnoteRefs`.
- Emit `app.metadataCache.getFileCache.referenceLinks` when a member read of `app.metadataCache.getFileCache.referenceLinks` on the rooted path `app.metadataCache.getFileCache.referenceLinks`.
- Emit `app.metadataCache.getFileCache.frontmatterLinks` when a member read of `app.metadataCache.getFileCache.frontmatterLinks` on the rooted path `app.metadataCache.getFileCache.frontmatterLinks`.
- Emit `app.metadataCache.getFileCache.frontmatter` when a member read of `app.metadataCache.getFileCache.frontmatter` on the rooted path `app.metadataCache.getFileCache.frontmatter`.
- Emit `app.metadataCache.getFileCache.frontmatterAliases` when a member read of `app.metadataCache.getFileCache.frontmatterAliases` on the rooted path `app.metadataCache.getFileCache.frontmatterAliases`.
- Emit `app.metadataCache.getFileCache.frontmatterTags` when a member read of `app.metadataCache.getFileCache.frontmatterTags` on the rooted path `app.metadataCache.getFileCache.frontmatterTags`.
- Emit `app.metadataCache.getFileCache.frontmatterPosition` when a member read of `app.metadataCache.getFileCache.frontmatterPosition` on the rooted path `app.metadataCache.getFileCache.frontmatterPosition`.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `tags`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `tags`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `links`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `links`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `embeds`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `embeds`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `blocks`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `blocks`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `headings`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `headings`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `sections`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `sections`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `listItems`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `listItems`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `footnotes`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `footnotes`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `footnoteRefs`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `footnoteRefs`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `referenceLinks`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `referenceLinks`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterLinks`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterLinks`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `frontmatter`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `frontmatter`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterAliases`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterAliases`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterTags`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterTags`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getFileCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterPosition`; event $0 has identity the rooted path `app.metadataCache.getFileCache`; object $1 is returned by the rooted path `app.metadataCache.getFileCache`; event $0 uses object $1 as its member receiver.
- Emit `app.metadataCache.getCache` when all of: event $0 is selected; event $0 is a member read of `frontmatterPosition`; event $0 has identity the rooted path `app.metadataCache.getCache`; object $1 is returned by the rooted path `app.metadataCache.getCache`; event $0 uses object $1 as its member receiver.

#### `obsidian:workspace.active-file`

Default severity: `info`

Queries:

- Emit `app.workspace.getActiveFile` when a member call to `app.workspace.getActiveFile` on the rooted path `app.workspace.getActiveFile`.

#### `obsidian:workspace.events`

Default severity: `info`

Queries:

- Emit `app.workspace.on` when a member call to `app.workspace.on` on the rooted path `app.workspace.on` with argument 0 matches one of the exact strings `active-leaf-change`, `css-change`, `editor-change`, `editor-drop`, `editor-menu`, `editor-paste`, `file-menu`, `file-open`, `files-menu`, `layout-change`, `quick-preview`, `quit`, `resize`, `url-menu`, `window-close`, `window-open`.

#### `obsidian:workspace.active-editor`

Default severity: `info`

Queries:

- Emit `app.workspace.activeEditor` when a member read of `app.workspace.activeEditor` on the rooted path `app.workspace.activeEditor`.

#### `obsidian:workspace.open`

Default severity: `info`

Queries:

- Emit `app.workspace.openLinkText` when a member call to `app.workspace.openLinkText` on the rooted path `app.workspace.openLinkText`.
- Emit `app.workspace.getLeaf.openFile` when a member call to `app.workspace.getLeaf.openFile` on the rooted path `app.workspace.getLeaf.openFile`.
- Emit `app.workspace.getLeaf` when all of: event $0 is selected; event $0 is a member call to `openFile`; event $0 has identity the rooted path `app.workspace.getLeaf`; object $1 is returned by the rooted path `app.workspace.getLeaf`; event $0 uses object $1 as its member receiver.
- Emit `app.workspace.getLeafById` when all of: event $0 is selected; event $0 is a member call to `openFile`; event $0 has identity the rooted path `app.workspace.getLeafById`; object $1 is returned by the rooted path `app.workspace.getLeafById`; event $0 uses object $1 as its member receiver.
- Emit `app.workspace.getLeftLeaf` when all of: event $0 is selected; event $0 is a member call to `openFile`; event $0 has identity the rooted path `app.workspace.getLeftLeaf`; object $1 is returned by the rooted path `app.workspace.getLeftLeaf`; event $0 uses object $1 as its member receiver.
- Emit `app.workspace.getRightLeaf` when all of: event $0 is selected; event $0 is a member call to `openFile`; event $0 has identity the rooted path `app.workspace.getRightLeaf`; object $1 is returned by the rooted path `app.workspace.getRightLeaf`; event $0 uses object $1 as its member receiver.
- Emit `app.workspace.ensureSideLeaf` when all of: event $0 is selected; event $0 is a member call to `openFile`; event $0 has identity the rooted path `app.workspace.ensureSideLeaf`; object $1 is returned by the rooted path `app.workspace.ensureSideLeaf`; event $0 uses object $1 as its member receiver.

#### `obsidian:workspace.leaf-management`

Default severity: `info`

Queries:

- Emit `app.workspace.getLeavesOfType` when a member call to `app.workspace.getLeavesOfType` on the rooted path `app.workspace.getLeavesOfType`.
- Emit `app.workspace.detachLeavesOfType` when a member call to `app.workspace.detachLeavesOfType` on the rooted path `app.workspace.detachLeavesOfType`.
- Emit `app.workspace.revealLeaf` when a member call to `app.workspace.revealLeaf` on the rooted path `app.workspace.revealLeaf`.
- Emit `app.workspace.getLeaf` when a member call to `app.workspace.getLeaf` on the rooted path `app.workspace.getLeaf`.
- Emit `app.workspace.getLeafById` when a member call to `app.workspace.getLeafById` on the rooted path `app.workspace.getLeafById`.
- Emit `app.workspace.getLeftLeaf` when a member call to `app.workspace.getLeftLeaf` on the rooted path `app.workspace.getLeftLeaf`.
- Emit `app.workspace.getRightLeaf` when a member call to `app.workspace.getRightLeaf` on the rooted path `app.workspace.getRightLeaf`.
- Emit `app.workspace.ensureSideLeaf` when a member call to `app.workspace.ensureSideLeaf` on the rooted path `app.workspace.ensureSideLeaf`.
- Emit `app.workspace.iterateRootLeaves` when a member call to `app.workspace.iterateRootLeaves` on the rooted path `app.workspace.iterateRootLeaves`.
- Emit `app.workspace.iterateAllLeaves` when a member call to `app.workspace.iterateAllLeaves` on the rooted path `app.workspace.iterateAllLeaves`.
- Emit `app.workspace.setActiveLeaf` when a member call to `app.workspace.setActiveLeaf` on the rooted path `app.workspace.setActiveLeaf`.
- Emit `app.workspace.moveLeafToPopout` when a member call to `app.workspace.moveLeafToPopout` on the rooted path `app.workspace.moveLeafToPopout`.
- Emit `app.workspace.openPopoutLeaf` when a member call to `app.workspace.openPopoutLeaf` on the rooted path `app.workspace.openPopoutLeaf`.

#### `obsidian:workspace.layout`

Default severity: `info`

Queries:

- Emit `app.workspace.getLayout` when a member call to `app.workspace.getLayout` on the rooted path `app.workspace.getLayout`.
- Emit `app.workspace.changeLayout` when a member call to `app.workspace.changeLayout` on the rooted path `app.workspace.changeLayout`.
- Emit `app.workspace.requestSaveLayout` when a member call to `app.workspace.requestSaveLayout` on the rooted path `app.workspace.requestSaveLayout`.

#### `obsidian:view.register`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerView`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:ui.command`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `addCommand`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:ui.ribbon`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `addRibbonIcon`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:ui.status-bar`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `addStatusBarItem`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:ui.modal`

Default severity: `info`

Queries:

- Emit `obsidian.Modal` when a constructor call to the `Modal` export from module `obsidian`.
- Emit `obsidian.Modal` when a class reference to the `Modal` export from module `obsidian`.

#### `obsidian:ui.notice`

Default severity: `info`

Queries:

- Emit `Notice` when a constructor call to the global `Notice`.
- Emit `obsidian.Notice` when a constructor call to the `Notice` export from module `obsidian`.
- Emit `obsidian.Notice` when a class reference to the `Notice` export from module `obsidian`.

#### `obsidian:ui.menu`

Default severity: `info`

Queries:

- Emit `obsidian.Menu` when all of: event $0 is selected; event $0 is a member call to `addItem`; event $0 has identity the `Menu` export from module `obsidian`; object $1 is constructed by the `Menu` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:ui.settings-tab`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `addSettingTab`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `PluginSettingTab` when a constructor call to the heuristic name `PluginSettingTab`.
- Emit `obsidian.PluginSettingTab` when a constructor call to the `PluginSettingTab` export from module `obsidian`.
- Emit `obsidian.PluginSettingTab` when a class reference to the `PluginSettingTab` export from module `obsidian`.

#### `obsidian:editor.content`

Default severity: `info`

Queries:

- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `getValue`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `setValue`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `getLine`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `setLine`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `getRange`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `replaceRange`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `getSelection`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `replaceSelection`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `getCursor`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `setCursor`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `setSelection`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Editor` when all of: event $0 is selected; event $0 is a member call to `setSelections`; event $0 has identity the `Editor` export from module `obsidian`; object $1 is constructed by the `Editor` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:editor.extension`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerEditorExtension`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:editor.suggest`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerEditorSuggest`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:file-manager.frontmatter-write`

Default severity: `info`

Queries:

- Emit `app.fileManager.processFrontMatter` when a member call to `app.fileManager.processFrontMatter` on the rooted path `app.fileManager.processFrontMatter`.

#### `obsidian:markdown.postprocessor`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerMarkdownPostProcessor`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:markdown.code-block-processor`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerMarkdownCodeBlockProcessor`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:markdown.render`

Default severity: `info`

Queries:

- Emit `obsidian` when a member call to `MarkdownRenderer.render` on the namespace imported from module `obsidian`.

#### `obsidian:markdown.link`

Default severity: `info`

Queries:

- Emit `obsidian` when a member call to `parseLinktext` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `normalizePath` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `getLinkpath` on the namespace imported from module `obsidian`.
- Emit `app.metadataCache.fileToLinktext` when a member call to `app.metadataCache.fileToLinktext` on the rooted path `app.metadataCache.fileToLinktext`.
- Emit `app.fileManager.generateMarkdownLink` when a member call to `app.fileManager.generateMarkdownLink` on the rooted path `app.fileManager.generateMarkdownLink`.
- Emit `obsidian` when a member call to `resolveSubpath` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `parseFrontMatterAliases` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member call to `parseFrontMatterTags` on the namespace imported from module `obsidian`.

#### `obsidian:codemirror.extension`

Default severity: `info`

Queries:

- Emit `@codemirror/state` when an import of the package specifier `@codemirror/state`.
- Emit `@codemirror/view` when an import of the package specifier `@codemirror/view`.
- Emit `@codemirror/language` when an import of the package specifier `@codemirror/language`.
- Emit `@codemirror/commands` when an import of the package specifier `@codemirror/commands`.
- Emit `@codemirror/lang-markdown` when an import of the package specifier `@codemirror/lang-markdown`.
- Emit `@codemirror/lang-javascript` when an import of the package specifier `@codemirror/lang-javascript`.
- Emit `@codemirror/lang-json` when an import of the package specifier `@codemirror/lang-json`.
- Emit `@codemirror/autocomplete` when an import of the package specifier `@codemirror/autocomplete`.
- Emit `@codemirror/lint` when an import of the package specifier `@codemirror/lint`.
- Emit `@codemirror/search` when an import of the package specifier `@codemirror/search`.
- Emit `@codemirror/collab` when an import of the package specifier `@codemirror/collab`.
- Emit `@lezer/common` when an import of the package specifier `@lezer/common`.
- Emit `@lezer/highlight` when an import of the package specifier `@lezer/highlight`.
- Emit `@lezer/lr` when an import of the package specifier `@lezer/lr`.
- Emit `@lezer/javascript` when an import of the package specifier `@lezer/javascript`.
- Emit `@lezer/markdown` when an import of the package specifier `@lezer/markdown`.

#### `obsidian:storage.app-data`

Default severity: `warning`

Queries:

- Emit `app.loadLocalStorage` when a member call to `app.loadLocalStorage` on the rooted path `app.loadLocalStorage`.
- Emit `app.saveLocalStorage` when a member call to `app.saveLocalStorage` on the rooted path `app.saveLocalStorage`.
- Emit `app.secretStorage.getSecret` when a member call to `app.secretStorage.getSecret` on the rooted path `app.secretStorage.getSecret`.
- Emit `app.secretStorage.setSecret` when a member call to `app.secretStorage.setSecret` on the rooted path `app.secretStorage.setSecret`.
- Emit `app.secretStorage.listSecrets` when a member call to `app.secretStorage.listSecrets` on the rooted path `app.secretStorage.listSecrets`.

#### `obsidian:storage.plugin-data-read`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `loadData`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:storage.plugin-data-write`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `saveData`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:lifecycle.events`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerEvent`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerDomEvent`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerInterval`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.
- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerObsidianProtocolHandler`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:bases.register`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerBasesView`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:cli.register`

Default severity: `info`

Queries:

- Emit `obsidian.Plugin` when all of: event $0 is selected; event $0 is a member call to `registerCliHandler`; event $0 has identity the `Plugin` export from module `obsidian`; object $1 is constructed by the `Plugin` export from module `obsidian`; event $0 uses object $1 as its member receiver.

#### `obsidian:platform.branching`

Default severity: `info`

Queries:

- Emit `obsidian` when a member read of `Platform.isMobile` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isDesktop` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isIosApp` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isAndroidApp` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isMacOS` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isWin` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isLinux` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isDesktopApp` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isMobileApp` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isPhone` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isTablet` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.isSafari` on the namespace imported from module `obsidian`.
- Emit `obsidian` when a member read of `Platform.resourcePathPrefix` on the namespace imported from module `obsidian`.

#### `obsidian:plugins.access`

Default severity: `info`

Queries:

- Emit `app.plugins.getPlugin` when a member call to `app.plugins.getPlugin` on the rooted path `app.plugins.getPlugin`.
- Emit `app.plugins.plugins` when a member read of `app.plugins.plugins` on the rooted path `app.plugins.plugins`.
- Emit `app.plugins.manifests` when a member read of `app.plugins.manifests` on the rooted path `app.plugins.manifests`.
- Emit `app.plugins.enabledPlugins` when a member read of `app.plugins.enabledPlugins` on the rooted path `app.plugins.enabledPlugins`.

#### `obsidian:plugins.enable-disable`

Default severity: `warning`

Queries:

- Emit `app.plugins.enablePlugin` when a member call to `app.plugins.enablePlugin` on the rooted path `app.plugins.enablePlugin`.
- Emit `app.plugins.disablePlugin` when a member call to `app.plugins.disablePlugin` on the rooted path `app.plugins.disablePlugin`.
- Emit `app.plugins.enablePluginAndSave` when a member call to `app.plugins.enablePluginAndSave` on the rooted path `app.plugins.enablePluginAndSave`.
- Emit `app.plugins.disablePluginAndSave` when a member call to `app.plugins.disablePluginAndSave` on the rooted path `app.plugins.disablePluginAndSave`.

#### `obsidian:plugins.load-unload`

Default severity: `warning`

Queries:

- Emit `app.plugins.loadPlugin` when a member call to `app.plugins.loadPlugin` on the rooted path `app.plugins.loadPlugin`.
- Emit `app.plugins.unloadPlugin` when a member call to `app.plugins.unloadPlugin` on the rooted path `app.plugins.unloadPlugin`.
- Emit `app.plugins.getPlugin` when all of: event $0 is selected; event $0 is a member call to `load`; event $0 has identity the rooted path `app.plugins.getPlugin`; object $1 is returned by the rooted path `app.plugins.getPlugin`; event $0 uses object $1 as its member receiver.
- Emit `app.plugins.getPlugin` when all of: event $0 is selected; event $0 is a member call to `unload`; event $0 has identity the rooted path `app.plugins.getPlugin`; object $1 is returned by the rooted path `app.plugins.getPlugin`; event $0 uses object $1 as its member receiver.
- Emit `app.plugins.plugins` when all of: event $0 is selected; event $0 is a member call to `load`; event $0 has identity the rooted path `app.plugins.plugins`; object $1 is returned by the rooted path `app.plugins.plugins`; event $0 uses object $1 as its member receiver.
- Emit `app.plugins.plugins` when all of: event $0 is selected; event $0 is a member call to `unload`; event $0 has identity the rooted path `app.plugins.plugins`; object $1 is returned by the rooted path `app.plugins.plugins`; event $0 uses object $1 as its member receiver.

